import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.debatemps.app',
  appName: 'debatemps',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
